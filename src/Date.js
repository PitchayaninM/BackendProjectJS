class DateRecord {
    constructor(id, borrowDate, returnDate) {
      this.id = id;
      this.borrowDate = borrowDate;
      this.returnDate = returnDate;
    }
  }
  
  module.exports = DateRecord;
  